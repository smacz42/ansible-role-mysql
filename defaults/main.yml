---

# core variables mysql
#mysql_dbuser:
#mysql_dbpasswd:
#mysql_dbname:
#
## Determines which schema to use in case this is called by another role
#mysql_schema:
#mysql_database:
#
## For postfix & dovecot deployment
#postfix_dovecot_mysql_virtual_domains:
#postfix_dovecot_mysql_virtual_users:
#postfix_dovecot_mysql_virtual_aliases:
